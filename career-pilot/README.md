# career-pilot Backend (Flask + JSON storage)

1. Replace app.py placeholder with the full app.py code you received earlier.
2. Install dependencies:
   pip install -r requirements.txt
3. Run backend:
   python app.py
